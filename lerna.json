{
  "npmClient": "yarn",
  "version": "independent",
  "useWorkspaces": true,
  "packages": [
    "plugins/*",
    "presets/*"
  ],
  "command": {
    "publish": {
      "npmClient": "npm",
      "verifyAccess": false,
      "ignoreChanges": ["*.md", "**/test/**"],
      "message": "chore(release): publish"
    }
  }
}
